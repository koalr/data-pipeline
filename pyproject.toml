[project]
name = "data-pipeline"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "sqlalchemy>=2.0",
    "pandas>=2.0",
    "click>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
